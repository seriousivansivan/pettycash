<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Edit Company</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 16px
        }

        header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 12px;
            gap: 8px;
            flex-wrap: wrap
        }

        nav a {
            margin-right: 8px;
            text-decoration: none;
            padding: 6px 10px;
            border-radius: 6px;
            background: #0d6efd;
            color: #fff
        }

        .msg {
            margin-bottom: 10px;
            padding: 8px 10px;
            border-radius: 6px
        }

        .ok {
            background: #e8f5e9;
            color: #1b5e20;
            border: 1px solid #a5d6a7
        }

        .err {
            background: #ffebee;
            color: #b71c1c;
            border: 1px solid #ef9a9a
        }

        .box {
            background: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 16px;
            max-width: 560px
        }

        label {
            display: block;
            margin-bottom: 10px
        }

        input[type=text],
        input[type=file] {
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 6px;
            width: 100%
        }

        img.logo {
            height: 48px;
            margin: 6px 0
        }

        button {
            padding: 8px 12px;
            border: 1px solid #ccc;
            background: #fff;
            border-radius: 6px;
            cursor: pointer
        }
    </style>
</head>

<body>
    <header>
        <div>
            <h2 style="margin:0">Edit Company</h2> <small>Logged in as <%= user.name %> (admin)</small>
        </div>
        <nav> <a href="/dashboard">Dashboard</a> <a href="/companies">Companies</a> <a href="/users">Users</a> <a
                href="/history">Histories</a> <a href="/profile">Profile</a> <a href="/logout"
                style="background:#6c757d">Logout</a> </nav>
    </header>
    <% if (message) { %>
        <div class="msg ok">
            <%= message %>
        </div>
        <% } %>
            <% if (error) { %>
                <div class="msg err">
                    <%= error %>
                </div>
                <% } %>

                    <div class="box">
                        <form method="POST" action="/companies/<%= company.id %>/edit" enctype="multipart/form-data">
                            <label>Company name <input type="text" name="name" value="<%= company.name %>" required>
                            </label>
                            <div>Current logo:</div>
                            <% if (company.logo_name) { %> <img class="logo" src="/logos/<%= company.logo_name %>"
                                    alt="">
                                <% } else { %>
                                    <div style="color:#6c757d">No logo uploaded.</div>
                                    <% } %> <label>Replace logo (PNG up to 10MB) <input type="file" name="logo"
                                                accept="image/png"> </label> <button type="submit">Save changes</button>
                        </form>
                    </div>
</body>

</html>